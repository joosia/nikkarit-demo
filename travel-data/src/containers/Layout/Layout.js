import React from 'react';
import './Layout.module.css';

const layout = (props) => {
    return (
        // React.Fragment on tässä tapauksessa Reactin vaatima ympäröivä komponentti, voisi olla myös esim div
        <React.Fragment>
            {/* Tähän rakennetaan leiska */}
        </React.Fragment>
    );
}

export default layout;